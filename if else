// Online C compiler to run C program online
#include <stdio.h>

int main() {
  int n1,n2 ;
  
  printf("enter a number is ");
  scanf("%d", & n1);
  
  printf("enetr a number is ");
  scanf("%d", & n2);
  
  if (n1==n2) {
      printf("the number is equal");
  
}

else {
    printf("the numbers are not equal ");
}
}
