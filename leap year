
// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
   int n1;
   printf("enter the year");
   scanf("%d", &n1);
   
   if (n1%4==0) {
       printf(" %d is the leap year",n1);
   }
     else {
         printf("it is not a leap year");
     }
   
}
